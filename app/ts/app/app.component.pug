doctype html

main
  h1 {{ title }}

  img(src="../../images/color-palette.png")   

  .formContainer
    app-single-form(*ngFor="let message of Messaging.messageList" '[message]'="message") 

  .messageContainer
    p.
      {{ jsonService }}
